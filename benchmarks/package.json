{
  "name": "@serde-wasm-bindgen/benches",
  "private": true,
  "dependencies": {
    "benchmark": "^2.1.4"
  },
  "scripts": {
    "pretest": "wasm-pack build -t nodejs --profiling",
    "test": "node index.js"
  }
}
